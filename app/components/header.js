export default function Footer() {
	return (
		<>
			{
				//too lazy to adjust css from boilerplate, so lets just hide the header for now
			}
			<h1 className="invisible">The price of $SOL</h1>
		</>
	);
}
